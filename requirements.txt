# No third-party Python dependencies.
# Local inference is served by llama-server (built from source in the image);
# both the local and Fireworks clients talk to their OpenAI-compatible HTTP
# endpoints using only the Python standard library (urllib + json).
